version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: test
      REDIS_HOST: redis
      CLUSTER_ID: ci-test-cluster
      SHADOW_MODE: 'true'
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
