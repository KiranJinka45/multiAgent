# Plan: Phase 13.1 — Staked Institutional Governance

## Tasks

### 1. Specification Finalization
- [ ] Update `docs/STAKED_GOVERNANCE_SPEC.md` with `StakeCertificate` schema.
- [ ] Add explicit `UnbondingPolicy` cooldown logic to the spec.

### 2. Staking Core Implementation
- [ ] Refactor `packages/staking-core/src/index.ts`:
    - [ ] Add `StakeCertificate` type.
    - [ ] Implement `CooldownTimer` logic in `StakeManager.initiateWithdrawal`.
    - [ ] Add `canWithdraw(fedId: string): boolean` check.
    - [ ] Implement `claim(fedId: string): void` to finalize withdrawal after cooldown.

### 3. CLI Integration
- [ ] Refactor `packages/ztanctl/src/index.ts`:
    - [ ] Import `StakeManager` from `@packages/staking-core`.
    - [ ] Connect `stake deposit` to `StakeManager.deposit`.
    - [ ] Connect `stake status` to `StakeManager.getStake`.
    - [ ] Connect `stake withdraw` to `StakeManager.initiateWithdrawal`.

### 4. Verification
- [ ] Create `packages/staking-core/test/staking.test.ts`.
- [ ] Create `test/integration/stake-integration.test.ts`.
- [ ] Run `snyk_code_scan` on the new code.

## Execution Waves

### Wave 1: Docs & Core
- `docs/STAKED_GOVERNANCE_SPEC.md`
- `packages/staking-core/src/index.ts`

### Wave 2: CLI & Integration
- `packages/ztanctl/src/index.ts`
- `test/integration/stake-integration.test.ts`

### Wave 3: Final Verification
- Unit tests
- Snyk scan
