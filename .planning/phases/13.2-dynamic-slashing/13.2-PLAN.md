# Plan: Phase 13.2 — Dynamic Slashing-as-a-Service

## Tasks

### 1. Staking Core Enhancements
- [ ] Refactor `packages/staking-core/src/index.ts`:
    - [ ] Add `slash(fedId: string, percentage: number): number` (returns amount slashed).
    - [ ] Update `StakeCertificate` if amount changes (or issue a "Slashing Event" artifact).

### 2. Slashing Engine Integration
- [ ] Refactor `packages/slashing-engine/src/index.ts`:
    - [ ] Add `executeSlashing(proof: SlashingProof, stakeManager: StakeManager): void`.
    - [ ] Implement Bounty Logic: Deduct 10% of slashed amount for the `reporterId`.

### 3. CLI Integration (ztanctl)
- [ ] Add `slash` command to `packages/ztanctl/src/index.ts`:
    - [ ] `report`: Submit evidence.
    - [ ] `status`: View slashing history for a federation.

### 4. Verification
- [ ] Create `test/integration/slashing-market.test.ts`.
- [ ] Simulate "Double Signing" and verify stake reduction and bounty distribution.

## Execution Waves

### Wave 1: Core Slashing Logic
- `StakeManager.slash`
- `SlashingEngine.executeSlashing`

### Wave 2: CLI & Market
- `ztanctl slash`
- Bounty tracking in `SlashingEngine`.

### Wave 3: Integration Test
- Full flow verification.
