---
wave: 1
depends_on: []
files_modified:
  - packages/evidence-lifecycle/src/crypto-utils.ts
  - packages/evidence-lifecycle/src/evidence-packet.ts
  - packages/federation/src/trust-registry.ts
  - packages/ztan-auditor/src/auditor.ts
  - packages/ztan-auditor/src/crypto-verification.test.ts
autonomous: true
requirements: []
---

# Plan 08.6-01: Trust Finality Verification

Implement empirical cryptographic validation for Trust Finality.

## Tasks

<task>
<action>
Create `packages/evidence-lifecycle/src/crypto-utils.ts`
- Expose functions to generate Ed25519 key pairs, sign payloads, and verify signatures using Node's native `crypto` module.
</action>
<acceptance_criteria>
- Utilities successfully generate keys and can verify their own signatures on stringified payloads.
</acceptance_criteria>
</task>

<task>
<action>
Update `packages/evidence-lifecycle/src/evidence-packet.ts`
- Extend `EvidencePacketGenerator` to cryptographically sign the pillars if a `privateKeyPem` is provided, instead of passing through raw string signatures.
</action>
<acceptance_criteria>
- A new method `generateSignedEnvelope` allows injecting real cryptographic signatures derived from a given private key.
</acceptance_criteria>
</task>

<task>
<action>
Update `packages/federation/src/trust-registry.ts`
- Upgrade to map `epochId` to an `operatorPublicKeys` array in addition to the root hash.
- Revocation acts on the PEM string of the public key.
</action>
<acceptance_criteria>
- `TrustRegistry` safely serializes and deserializes the public keys and revoked keys.
</acceptance_criteria>
</task>

<task>
<action>
Update `packages/ztan-auditor/src/auditor.ts`
- Refactor the verification logic to use real cryptographic signature verification instead of string matching.
- Ensure revoked public keys are explicitly rejected cryptographically.
</action>
<acceptance_criteria>
- Rejects envelopes where `crypto.verify` fails or the signing key is marked revoked.
</acceptance_criteria>
</task>

<task>
<action>
Create `packages/ztan-auditor/src/crypto-verification.test.ts`
- Execute the exact 8-step verification wave mandated by the audit.
</action>
<acceptance_criteria>
- End-to-end cryptographic trust chain validates correctly.
- Key revocation correctly invalidates the same previously-valid packet.
</acceptance_criteria>
</task>

## Verification
- `npx vitest run packages/ztan-auditor/src/crypto-verification.test.ts`
