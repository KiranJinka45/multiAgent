# Plan: Phase 13.3 — Global Governance Settlement Layer

## Tasks

### 1. Settlement Package Initialization
- [ ] Create `packages/settlement-layer/package.json`.
- [ ] Create `packages/settlement-layer/src/index.ts`.
- [ ] Implement `SettlementManager` with `settleEvent(proof: SlashingProof): Promise<string>`.

### 2. Proof Generation
- [ ] Implement `ProofGenerator` in `packages/settlement-layer/src/proofs.ts`:
    - [ ] Merkle tree construction of slashing events.
    - [ ] Proof of inclusion generation.

### 3. CLI Integration (ztanctl)
- [ ] Add `settle` command to `packages/ztanctl/src/index.ts`:
    - [ ] `status`: View the state of global settlement for recent breaches.
    - [ ] `history`: View immutable settlement logs.

### 4. Verification
- [ ] Integration test: Full flow from Slashing to Global Settlement.
- [ ] Snyk scan of the new package.

## Execution Waves

### Wave 1: Core Settlement
- `packages/settlement-layer` skeleton.
- `SettlementManager` implementation.

### Wave 2: Proofs & CLI
- Merkle proof generation.
- `ztanctl settle` command wiring.

### Wave 3: Final Verification
- Integration test and security scan.
