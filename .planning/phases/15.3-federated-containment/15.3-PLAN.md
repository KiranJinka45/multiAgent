# Plan: Phase 15.3 — Federated Trust Containment & Reputation Quarantine

## Tasks

### 1. Federated Trust Containment
- [ ] Create `packages/federated-containment/index.ts`:
    - [ ] `detectAnomaly(attestation: any): boolean`.
    - [ ] `quarantineFederation(orgId: string): void`.

### 2. Reputation Quarantine
- [ ] Create `packages/reputation-quarantine/index.ts`:
    - [ ] `downgradeReputation(orgId: string, reason: string): void`.
    - [ ] `checkRecoveryEligibility(orgId: string): boolean`.

### 3. Federated Recovery
- [ ] Create `packages/federated-recovery/index.ts`:
    - [ ] `rebuildTrustLineage(orgId: string): boolean`.
    - [ ] `reCertifyLegitimacy(orgId: string): string`.

### 4. Cross-Federation Drift Analysis
- [ ] Create `packages/federated-drift/index.ts`:
    - [ ] `analyzeCrossOrgDrift(orgA: any, orgB: any): DriftReport`.

### 5. Federated CLI Integration (ztanctl)
- [ ] Add `federation` expansion commands:
    - [ ] `ztanctl federation quarantine <orgId>`
    - [ ] `ztanctl federation downgrade <orgId>`
    - [ ] `ztanctl federation recover <orgId>`
    - [ ] `ztanctl federation drift <orgId>`

### 6. Documentation
- [ ] Create `docs/FEDERATED_CONTAINMENT_SPEC.md`.
- [ ] Create `docs/REPUTATION_QUARANTINE_MODEL.md`.
- [ ] Create `docs/FEDERATED_RECOVERY_WORKFLOW.md`.

### 7. Verification
- [ ] Simulate compromised federation replay corruption.
- [ ] Verify containment of trust contagion.
- [ ] Demonstrate post-quarantine recovery of a degraded federation.

## Execution Waves

### Wave 1: Containment & Quarantine
- Anomaly detection and staged degradation logic.

### Wave 2: Recovery & CLI
- Trust rebuilding and `ztanctl federation` command expansion.

### Wave 3: Docs & Chaos Simulation
- Final containment documentation.
- Federated "chaos" simulation run (poisoned attestations).
