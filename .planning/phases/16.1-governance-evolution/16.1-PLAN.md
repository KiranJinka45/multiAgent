# Plan: Phase 16.1 — Bounded Constitutional Mutation

## Tasks

### 1. Governance Evolution Engine
- [ ] Create `packages/governance-evolution/index.ts`:
    - [ ] `proposeUpgrade(policyChange: any): string`.
    - [ ] `validateMutation(proposalId: string): boolean`.

### 2. Continuity Migration
- [ ] Create `packages/continuity-migration/index.ts`:
    - [ ] `bridgeContext(oldState: any, newPolicy: any): any`.
    - [ ] `generateMigrationProof(oldHash: string, newHash: string): string`.

### 3. Upgrade Replay Engine
- [ ] Create `packages/upgrade-replay/index.ts`:
    - [ ] `verifyUpgradeLineage(upgradeEvent: any): boolean`.
    - [ ] `replayAcrossPolicyBoundary(events: any[]): boolean`.

### 4. Governance CLI Integration (ztanctl)
- [ ] Add `governance` expansion commands:
    - [ ] `ztanctl governance propose <policyFile>`
    - [ ] `ztanctl governance upgrade <proposalId>`
    - [ ] `ztanctl governance migrate`
    - [ ] `ztanctl governance history`

### 5. Documentation
- [ ] Create `docs/ADAPTIVE_CONSTITUTION_SPEC.md`.
- [ ] Create `docs/UPGRADE_REPLAY_MODEL.md`.
- [ ] Create `docs/EVOLUTIONARY_TRUST_STRATEGY.md`.

### 6. Verification
- [ ] Simulate constitutional policy upgrade.
- [ ] Verify continuity of replay across the policy boundary.
- [ ] Validate that federated peers can verify the upgrade legitimacy.

## Execution Waves

### Wave 1: Mutation & Migration
- Upgrade proposal logic and context bridging.

### Wave 2: Replay & CLI
- Cross-boundary replay verification and `ztanctl governance` commands.

### Wave 3: Docs & Evolution Simulation
- Final evolution documentation.
- Simulated "Constitutional Upgrade" run.
