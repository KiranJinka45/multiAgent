---
wave: 1
depends_on: []
files_modified:
  - packages/sandbox/src/manager.test.ts
  - packages/sandbox/fixtures/pilot-remediation.json
  - packages/sandbox/fixtures/pilot-cve.json
autonomous: true
requirements:
  - PILOT-01
  - PILOT-02
---

# Plan 08.1-01: Pilot Infrastructure & Fixtures

Setup the necessary fixtures and test stubs to validate pilot isolation and replay semantics.

## Tasks

<task>
<read_first>
- packages/sandbox/src/types.ts
- packages/sandbox/src/manager.ts
</read_first>
<action>
Create `packages/sandbox/fixtures/pilot-remediation.json` with a profile for dependency remediation.
- runtime: "gvisor"
- mounts: [{ source: "vfs://src", target: "/app", mode: "ro" }]
- network: { outbound: true }
- resources: { cpu: "500m", memory: "512Mi", timeoutSeconds: 300 }
</action>
<acceptance_criteria>
- File exists: packages/sandbox/fixtures/pilot-remediation.json
- JSON contains `runtime: "gvisor"` and `mode: "ro"`
</acceptance_criteria>
</task>

<task>
<read_first>
- packages/sandbox/src/types.ts
</read_first>
<action>
Create `packages/sandbox/fixtures/pilot-cve.json` with a profile for CVE reproduction.
- runtime: "firecracker"
- network: { outbound: false }
- resources: { cpu: "1.0", memory: "1Gi", timeoutSeconds: 60 }
</action>
<acceptance_criteria>
- File exists: packages/sandbox/fixtures/pilot-cve.json
- JSON contains `runtime: "firecracker"` and `outbound: false`
</acceptance_criteria>
</task>

<task>
<read_first>
- packages/sandbox/src/manager.ts
</read_first>
<action>
Create `packages/sandbox/src/manager.test.ts` with integration tests for isolation and replay.
- Test that "gvisor" provider correctly handles the remediation fixture.
- Test that "firecracker" provider rejects forbidden outbound traffic.
- Test that `mountHash` and `executionHash` are correctly calculated by the manager.
</action>
<acceptance_criteria>
- File exists: packages/sandbox/src/manager.test.ts
- `pnpm test packages/sandbox/src/manager.test.ts` passes (using MockProvider where necessary)
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- `pnpm test packages/sandbox/src/manager.test.ts`

### Manual Verification
- None required for Wave 1.

## must_haves
- [ ] Sandbox profiles for both pilots exist as JSON fixtures.
- [ ] Integration tests verify isolation and replay hash stability.
