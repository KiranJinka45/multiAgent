---
wave: 2
depends_on:
  - 08.1-01-PILOT-SETUP-PLAN.md
files_modified:
  - packages/core-engine/src/mission-orchestrator.ts
  - packages/core-engine/src/mission-orchestrator.test.ts
autonomous: true
requirements:
  - PILOT-03
  - PILOT-04
---

# Plan 08.1-02: Pilot Execution & Gating

Implement the pilot mission logic, human-in-the-loop gates, and cost ceiling enforcement.

## Tasks

<task>
<read_first>
- packages/core-engine/src/mission-orchestrator.ts
- packages/sandbox/src/types.ts
</read_first>
<action>
Update `MissionOrchestrator` to support `PilotGate` transitions.
- Add `processPilotGate(missionId: string, gate: PilotGate): Promise<void>` method.
- Missions in `READY` state must wait for `PilotGate.decision === 'APPROVE'` before transitioning to `EXECUTING`.
- Log the decision rationale in the mission metadata.
</action>
<acceptance_criteria>
- `mission-orchestrator.ts` contains `processPilotGate` method.
- Mission state machine correctly gates transitions from `READY` to `EXECUTING`.
</acceptance_criteria>
</task>

<task>
<read_first>
- packages/core-engine/src/mission-orchestrator.ts
- packages/billing/src/index.ts (if exists, or mock)
</read_first>
<action>
Implement cost ceiling enforcement in `MissionOrchestrator`.
- Check available project credits before provisioning a sandbox.
- If credits < required_for_mission, transition mission to `FAILED` with reason `INSUFFICIENT_FUNDS`.
- Integrate with `SandboxManager.destroy()` if mission duration exceeds quota.
</action>
<acceptance_criteria>
- `mission-orchestrator.ts` checks credits before `sandboxManager.provision`.
- Mission fails gracefully on zero balance.
</acceptance_criteria>
</task>

<task>
<read_first>
- packages/core-engine/src/mission-orchestrator.ts
</read_first>
<action>
Create `packages/core-engine/src/mission-orchestrator.test.ts` to validate gates and cost ceilings.
- Mock `SandboxManager` to return successful executions.
- Test that a mission remains `READY` until approved.
- Test that a mission fails if credits are zero.
</action>
<acceptance_criteria>
- File exists: packages/core-engine/src/mission-orchestrator.test.ts
- `pnpm test packages/core-engine/src/mission-orchestrator.test.ts` passes.
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- `pnpm test packages/core-engine/src/mission-orchestrator.test.ts`

### Manual Verification
- Start a test mission, verify it stays `READY`.
- Manually approve the gate, verify it proceeds to `EXECUTING`.

## must_haves
- [ ] HITL gate logic prevents unauthorized mission execution.
- [ ] Cost ceilings and credit checks prevent unbudgeted resource consumption.
- [ ] Integration tests cover both success and failure (gate rejection/zero balance) paths.
