---
wave: 1
depends_on: []
files_modified:
  - packages/federation/src/trust-registry.ts
  - packages/evidence-lifecycle/src/evidence-packet.ts
  - packages/federation/src/cell.ts
  - packages/ztan-auditor/src/auditor.ts
autonomous: true
requirements: []
---

# Plan 08.4-01: Trust Root Hardening

Implement Key Revocations, Governance Epochs, and the offline ztan-auditor CLI.

## Tasks

<task>
<action>
Create `packages/federation/src/trust-registry.ts` and `trust-registry.test.ts`.
- Implement `TrustRegistry` class to manage Governance Epochs and Revocation List.
</action>
<acceptance_criteria>
- Class handles storing epoch-to-root mappings.
- Class handles storing and checking revoked signatures.
</acceptance_criteria>
</task>

<task>
<action>
Update `packages/evidence-lifecycle/src/evidence-packet.ts` and `evidence-packet.test.ts`.
- Add `governanceEpoch` field to `FinalizedEnvelope`.
</action>
<acceptance_criteria>
- Tests pass.
</acceptance_criteria>
</task>

<task>
<action>
Update `packages/federation/src/cell.ts` and `cell.test.ts`.
- Integrate `TrustRegistry` to `ExecutionCell`.
- Reject packets matching revoked signatures or unknown epochs.
</action>
<acceptance_criteria>
- Tests pass.
</acceptance_criteria>
</task>

<task>
<action>
Create `packages/ztan-auditor/src/auditor.ts` and `auditor.test.ts`.
- Implement offline verification engine.
</action>
<acceptance_criteria>
- Offline validation works with mock signatures and structural checks.
- Tests pass.
</acceptance_criteria>
</task>

## Verification
- `npx vitest run packages/federation/src/trust-registry.test.ts`
- `npx vitest run packages/ztan-auditor/src/auditor.test.ts`
