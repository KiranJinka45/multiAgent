/**
 * Module augmentation for @packages/utils.
 *
 * IMPORTANT: This file must remain a module (via the `export {}` statement)
 * so that `declare module` performs module augmentation (merging with the
 * real module's types) rather than creating an ambient external module
 * that would shadow and replace them.
 */
export {};
