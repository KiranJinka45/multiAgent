/**
 * CORE ENGINE - TEMPLATE REGISTRY MOCK
 * Harmonized with the Universal Bridge.
 */
export const templateRegistry = {
    getTemplate: (id: string) => ({ id, name: 'Mock Template' }),
    listTemplates: () => [],
};


