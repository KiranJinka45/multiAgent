apiVersion: v1
kind: ServiceAccount
metadata:
  name: multiagent-sa
  namespace: default
  annotations:
    # REPLACE with output from 'terraform output secrets_role_arn'
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/multiagent-secrets-role
